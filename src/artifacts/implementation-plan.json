{
  "slug": "implementation-plan",
  "title": "Visual Artifact Renderer Plan",
  "description": "A non-report artifact rendered by the same JSON DSL.",
  "data": {
    "phases": [
      { "phase": "Schema", "owner": "App", "status": "Done" },
      { "phase": "Renderer", "owner": "App", "status": "Done" },
      { "phase": "PI tool", "owner": "Agent", "status": "Next" }
    ]
  },
  "nodes": [
    {
      "type": "section",
      "props": {
        "title": "Goal",
        "description": "Make visual pages from specs instead of generated React files."
      },
      "children": [
        { "type": "text", "props": { "text": "The LLM emits a constrained JSON tree. The app validates it, saves it, and renders it through a stable component registry.", "size": "lg" } }
      ]
    },
    {
      "type": "grid",
      "props": { "columns": 3 },
      "children": [
        {
          "type": "card",
          "props": { "title": "Input" },
          "children": [
            { "type": "text", "props": { "text": "A JSON spec with title, data, and visual nodes.", "tone": "muted" } }
          ]
        },
        {
          "type": "card",
          "props": { "title": "Runtime" },
          "children": [
            { "type": "text", "props": { "text": "One dynamic route reads the spec and renders known adapters.", "tone": "muted" } }
          ]
        },
        {
          "type": "card",
          "props": { "title": "Output" },
          "children": [
            { "type": "text", "props": { "text": "A visual page at /artifacts/[slug].", "tone": "muted" } }
          ]
        }
      ]
    },
    {
      "type": "card",
      "props": { "title": "Execution status" },
      "children": [
        { "type": "table", "props": { "dataKey": "phases", "columns": ["phase", "owner", "status"] } }
      ]
    },
    {
      "type": "accordion",
      "props": {
        "items": [
          {
            "title": "Why not generate route files?",
            "nodes": [
              { "type": "text", "props": { "text": "Generated routes couple artifact creation to source changes. Specs are content, so they should stay data." } }
            ]
          },
          {
            "title": "What is still missing?",
            "nodes": [
              { "type": "text", "props": { "text": "A real PI tool wrapper can call create_visual_artifact once the extension location is chosen.", "tone": "muted" } }
            ]
          }
        ]
      }
    }
  ]
}
