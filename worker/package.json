{
  "name": "@agents/visual-artifact-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke:deploy": "bun run scripts/smoke-deploy.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "bun-types": "latest",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
